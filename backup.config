# === backup.config ===
# Where backups should be stored
BACKUP_DESTINATION="$HOME/backup-system/backups"

# Which folders to skip during backup
EXCLUDE_PATTERNS=".git,node_modules,.cache"

# How many backups to keep
DAILY_KEEP=7
WEEKLY_KEEP=4
MONTHLY_KEEP=3

# Checksum type
CHECKSUM_CMD="sha256sum"

# Logging and lock settings
LOG_FILE="$HOME/backup-system/backup.log"
LOCK_FILE="/tmp/backup.lock"

# Optional email (for simulation)
EMAIL=""
